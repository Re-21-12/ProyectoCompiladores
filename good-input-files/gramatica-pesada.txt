hola {
    contador entero = 0;
    suma entero = 0;
    suma2 entero = 0;
    mensaje cadena = "Comenzando algoritmo";
    es_valido bool = verdadero;
    es_admin bool = falso;
    
    contador2 entero = 0;
    suma3 entero = 0;
    suma4 entero = 0;
    mensaje1 cadena = "Comenzando algoritmo";
    es_valido2 bool = verdadero;

    mostrar(mensaje);

    resultado1 entero = 5 + 3;
    resultado2 entero = 10 - 2;
    resultado3 entero = 4 * 7;
    resultado4 decimal = 20.0 / 4.0;

    mostrar(resultado1);
    mostrar(resultado2);
    mostrar(resultado3);
    mostrar(resultado4);

    total entero = resultado1 + resultado2 ;
    total2 entero = resultado3 * resultado1 ;
    mostrar(total);

    if(es_valido){
        mensaje cadena = "Usuario válido";
        mostrar(mensaje);

        if(es_admin){
            mostrar("Bienvenido administrador");
        } else {
            mostrar("Acceso limitado");
        }
    }

    for (i entero = 0; i < 19; i++) {
        suma = suma + i;
        mostrar(suma);
    }

    while(suma < 20){
        suma++;
        mostrar(suma);
    }

    funcion calcularMultiplicacion entero (a entero, b entero){
        resultado entero = a * b;
        mostrar(resultado);
        retorna resultado;
    }

    funcion calcularPromedio decimal (total decimal, cantidad decimal){
        promedio decimal = total / cantidad;
        mostrar(promedio);
        retorna promedio;
    }

    calcularMultiplicacion(4, 6);
    calcularPromedio(35.5, 5.0);

    funcion esParFunc bool (numero entero){
        if(numero > 0){
            retorna verdadero;
        } else {
            retorna falso;
        }
    }

    par bool = verdadero;
    if(par){
        mostrar("El resultado es par");
    } else {
        mostrar("El resultado es impar");
    }

    for (x entero = 0; x < 1000; x++) {
        suma2 = suma2 + x;
        mostrar(suma2);
    }
}
