hola {
    contador entero = 0;
    suma entero = 0;
    mensaje cadena = "Comenzando algoritmo";
    es_valido bool = verdadero;
    es_admin bool = falso;

    mostrar(mensaje);

    if(es_valido){
        mensaje cadena = "Usuario válido";
        mostrar(mensaje);

        if(es_admin){
            mostrar("Bienvenido administrador");
        } else {
            mostrar("Acceso limitado");
        }
    }

    for (i entero = 0; i < 5; i++) {
        suma = suma + i;
        mostrar(suma);
    }

    while(suma < 20){
        suma++;
        mostrar(suma);
    }

    funcion calcularMultiplicacion entero (a entero, b entero){
        resultado entero = a * b;
        mostrar(resultado);
        retorna resultado;
    }

    funcion calcularPromedio decimal (total decimal, cantidad decimal){
        promedio decimal = total / cantidad;
        mostrar(promedio);
        retorna promedio;
    }

    funcion esPar bool (numero entero){
        if((numero % 2) == 0){
            retorna verdadero;
        } else {
            retorna falso;
        }
    }

    resultado entero = calcularMultiplicacion(4, 6);
    mostrar(resultado);

    promedio_final decimal = calcularPromedio(35.5, 5.0);
    mostrar(promedio_final);

    par bool = esPar(resultado);
    if(par){
        mostrar("El resultado es par");
    } else {
        mostrar("El resultado es impar");
    }
}
